@model Assignment
@{
    ViewData["Title"] = localizer["Create Assignment"];
}

<h1>@ViewData["Title"]</h1>

<p>@Html.ValidationMessage("Assignment", new { @class = "text-danger" })</p>

<br />
@Html.DisplayFor(model => model, "Breadcrumbs", new { Controller = "Assignment", ControllerText = localizer["Assignments"], CurrentPage = ViewData["Title"] })

<h4>@localizer["Assignment"]</h4>
<hr />
<form asp-action="Create">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="form-floating mb-3">
        <select asp-for="DeviceUuid" class="form-control" asp-items="@(ViewBag.Devices)" placeholder="Assignment device UUID">
            <option value="" disabled selected>@localizer["Please select one"]</option>
        </select>
        <label asp-for="DeviceUuid"></label>
        <span asp-validation-for="DeviceUuid" class="text-danger"></span>
    </div>
    @Html.EditorForModel("EditAssignmentDetails")
    <div class="form-group">
        <input type="submit" value="@localizer["Create"]" class="btn btn-primary" />
    </div>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}