@using Microsoft.AspNetCore.Identity
@using ChuckDeviceConfigurator.Data
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item" style="align-self: center;">
        @if (UserManager.GetUserAsync(User)?.Result?.ProfilePicture != null)
        {
            <img style="width:40px;height:40px; object-fit:cover; border-radius:30px" src="data:image/*;base64,@(Convert.ToBase64String(UserManager.GetUserAsync(User).Result.ProfilePicture))">
        }
    </li>
    <li class="nav-item">
        <a href="#" data-toggle="collapse" data-bs-toggle="collapse" data-bs-target="#userSubmenu" aria-expanded="false" class="dropdown-toggle collpased">
            <i class="fas fa-user-circle"></i>
            &nbsp;@User.Identity?.Name
        </a>
        <ul class="collapse list-unstyled align-text-left" id="userSubmenu">
            <li>
                <a class="dropdown-item text-light" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage Account">
                    <i class="fa-solid fa-fw fa-address-card"></i>
                    Account
                </a>
            </li>
            <li>
                <form class="form-inline dropdown-item" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                    <button type="submit" class="nav-link btn btn-sm btn-link text-light" title="Logout">
                        <i class="fa-solid fa-fw fa-arrow-right-from-bracket"></i>
                        Logout
                    </button>
                </form>
            </li>
        </ul>
    </li>
}
else
{
    <li class="nav-item">
        <a class="nav-link text-light" asp-area="Identity" asp-page="/Account/Register">
            <i class="fas fa-fw fa-network-wired"></i>
            Register
        </a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-light" asp-area="Identity" asp-page="/Account/Login">
            <i class="fas fa-fw fa-network-wired"></i>
            Login
        </a>
    </li>
}
</ul>