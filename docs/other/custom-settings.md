# Custom Settings  

Any custom settings introduced from plugins using the `IUiHost` host handler are read from and saved to the following location:  
`./src/ChuckDeviceConfigurator/bin/Debug/settings.json`  

